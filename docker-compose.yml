services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: foodly
      POSTGRES_USER: foodly
      POSTGRES_PASSWORD: foodly_secret
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U foodly"]
      interval: 5s
      timeout: 5s
      retries: 5

  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    environment:
      DATABASE_URL: postgresql://foodly:foodly_secret@postgres:5432/foodly
      TYPEORM_SYNC: "true"
      CORS_ORIGINS: http://localhost:80,http://localhost:5173
      JWT_SECRET: change-in-production
    ports:
      - "3000:3000"
    depends_on:
      postgres:
        condition: service_healthy

  client:
    build:
      context: ./client
      dockerfile: Dockerfile
    ports:
      - "80:80"
    depends_on:
      - server

volumes:
  pgdata:
