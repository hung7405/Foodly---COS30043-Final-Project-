"""Foodly Code Intelligence Layer.

A lightweight repository scanner + code graph builder that produces a compact
machine-readable map of the codebase so LLM coding agents (OpenCode) can query
structure, dependencies and code flows without reading every file.
"""

__version__ = "1.0.0"
